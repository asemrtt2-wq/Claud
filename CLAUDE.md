# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository (`asemrtt2-wq/claud`) is currently empty: no commits, no source files, no build configuration, and no README exist yet. There is no established architecture, toolchain, or convention to document.

## Guidance for future work

- Once source files, a package manifest (e.g. `package.json`, `pyproject.toml`, `go.mod`, `Cargo.toml`), or a README are added, update this file to describe:
  - The actual build, lint, and test commands (and how to run a single test).
  - The high-level architecture and how major components interact.
  - Any project-specific conventions discovered in the code, README, or `.cursor/rules`, `.cursorrules`, or `.github/copilot-instructions.md` files.
- Do not invent commands, frameworks, or structure that aren't backed by files actually present in the repository.
